return {
  parameters: [
    { name: "edge1", default:{} },
    { name: "edge2", default:{} },
    { name: "offset", default:0 },
  ],
  func: function (sketch, edge1, edge2, offset) {

      const dim = item.segment.vertices[1], item.segment.vertices[0], "right");
      sketch.add(dim);


    })
    return sketch;
  }
}

